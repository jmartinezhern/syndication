language: go

go:
  - 1.x

before_install:
  - go get github.com/alecthomas/gometalinter/...
  - gometalinter --install

script:
  - ./test.sh

after_success:
  - bash <(curl -s https://codecov.io/bash)
